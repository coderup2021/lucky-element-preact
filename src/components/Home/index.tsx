import { h, render, FunctionComponent } from "preact";
import Button from "../Button";
const Home: FunctionComponent = () => {
  return (
    <>
      <Button>1234</Button>
      this is home page
    </>
  );
};

export default Home;
